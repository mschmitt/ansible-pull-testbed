# Post-execution reporting
- hosts: localhost
  gather_facts: false
  connection: local
  tasks:
  - name: Remove log file
    file:
      path: /tmp/ansible-pull.d/localhost
      state: absent
# vim: filetype=yaml
