---
- name: Get stdout
  shell: uname -a
  register: stdout
- name: Save output to file
  copy:
    content: "{{ stdout }}"
    dest: /tmp/stdout
- name: Save facts to file
  copy:
    content: "{{ ansible_facts }}"
    dest: /tmp/facts

# vim: filetype=yaml
